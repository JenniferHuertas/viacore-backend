export enum RequestStatus {
PENDING = 'pending',
  IN_REVIEW = 'in_review',
  AWAITING_PAYMENT = 'awaiting_payment',
  SCHEDULED = 'scheduled',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled'     
}